spring:
  application:
    name: talk
  data:
    mongodb:
      uri: mongodb+srv://${MONGO_ROOT_USERNAME:warcraftkun}:${MONGO_ROOT_PASSWORD:CmeYUkN0DJHeVBmH}@cluster0-baole.6bydbes.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0-BaoLe
  datasource:
    url: jdbc:postgresql://postgres:5432/${POSTGRES_DB:talktalk}
    username: ${POSTGRES_USER:postgres}
    password: ${POSTGRES_PASSWORD:123456}
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
  redis:
    host: redis
    port: 6379
